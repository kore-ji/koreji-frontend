import { Dimensions } from 'react-native';

export const { height: SCREEN_HEIGHT } = Dimensions.get('window');
export const COLLAPSED_HEIGHT = 80;
export const EXPANDED_HEIGHT = SCREEN_HEIGHT * 0.5;
export const HEADER_HEIGHT = 60;

